# 🛠️ Инструменты разработки DataCode

Этот раздел демонстрирует инструменты для разработки, отладки и тестирования производительности в DataCode.

## 📋 Содержание

### 1. `debug_mode_test.dc` - Тестирование режима отладки
**Описание**: Демонстрирует возможности отладки и диагностики в DataCode.

**Что изучается**:
- Режим отладки
- Диагностические сообщения
- Трассировка выполнения
- Отладочная информация

**Как использовать**:
```bash
# Запуск в обычном режиме
cargo run examples/06-инструменты-разработки/debug_mode_test.dc

# Запуск с отладочной информацией (если поддерживается)
cargo run -- --debug examples/06-инструменты-разработки/debug_mode_test.dc
```

### 2. `interactive_demo.dc` - Интерактивные демонстрации
**Описание**: Примеры кода, предназначенные для выполнения в интерактивном режиме (REPL).

**Что изучается**:
- Работа в REPL режиме
- Интерактивное тестирование
- Пошаговое выполнение
- Экспериментирование с кодом

**Как использовать**:
```bash
# Запустите интерактивный режим
cargo run

# Затем копируйте и вставляйте код из файла
DataCode> # код из interactive_demo.dc
```

### 3. `stress_benchmark.dc` - Тестирование производительности
**Описание**: Бенчмарки для тестирования производительности различных операций DataCode.

**Что изучается**:
- Измерение производительности
- Стресс-тестирование
- Оптимизация кода
- Анализ узких мест

**Предупреждение**: ⚠️ Этот файл может выполняться долго и потреблять много ресурсов!

## 🎯 Как запускать примеры

```bash
# Тестирование отладки
cargo run examples/06-инструменты-разработки/debug_mode_test.dc

# Интерактивные примеры (запустите REPL)
cargo run
# Затем используйте код из interactive_demo.dc

# Бенчмарки (будьте осторожны!)
cargo run examples/06-инструменты-разработки/stress_benchmark.dc
```

## 🔧 Инструменты отладки

### Режим отладки
```datacode
# Добавьте отладочные print() для трассировки
global function debug_function(x) do
    print('DEBUG: Входное значение:', x)
    local result = x * 2
    print('DEBUG: Результат:', result)
    return result
endfunction
```

### Проверка состояния
```datacode
# Проверка переменных в критических точках
global x = 10
print('Состояние x:', x)
if x > 5 do
    print('DEBUG: x больше 5')
endif
```

### Трассировка выполнения
```datacode
global function traced_factorial(n) do
    print('TRACE: factorial(', n, ') вызвана')
    if n <= 1 do
        print('TRACE: базовый случай, возвращаем 1')
        return 1
    endif
    local result = n * traced_factorial(n - 1)
    print('TRACE: factorial(', n, ') = ', result)
    return result
endfunction
```

## 📊 Тестирование производительности

### Измерение времени выполнения
```datacode
# Используйте функцию now() для измерения времени
global start_time = now()

# Ваш код для тестирования
for i in [1, 2, 3, 4, 5] do
    # некоторые операции
forend

global end_time = now()
print('Время выполнения:', end_time - start_time)
```

### Стресс-тесты
```datacode
# Тестирование с большими данными
global large_array = []
for i in range(1000) do
    push(large_array, i)
forend

# Тестирование производительности операций
global sum_result = sum(large_array)
```

## 🔍 Интерактивная разработка

### REPL режим
```bash
cargo run
DataCode> global x = 10
DataCode> print(x)
10
DataCode> global function test() do return x * 2 endfunction
DataCode> print(test())
20
```

### Пошаговое тестирование
1. Запустите REPL: `cargo run`
2. Определите переменные: `global x = 5`
3. Тестируйте функции: `print(x + 10)`
4. Экспериментируйте с кодом
5. Используйте историю команд (стрелки вверх/вниз)

## ⚠️ Важные особенности

### Отладка
- Используйте `print()` для вывода отладочной информации
- Добавляйте префиксы `DEBUG:` или `TRACE:` для ясности
- Проверяйте состояние переменных в критических точках

### Производительность
- `stress_benchmark.dc` может выполняться очень долго
- Следите за потреблением памяти при больших данных
- Используйте `now()` для измерения времени

### Интерактивный режим
- REPL поддерживает историю команд
- Можно определять функции и переменные пошагово
- Используйте для быстрого тестирования идей

## 💡 Практические советы

1. **Отладка**: Добавляйте временные print() для понимания потока выполнения
2. **REPL**: Используйте для быстрого тестирования небольших фрагментов кода
3. **Бенчмарки**: Запускайте на небольших данных сначала
4. **Профилирование**: Измеряйте время критических операций
5. **Итеративная разработка**: Тестируйте код по частям

## 🔗 Навигация

### Предыдущие разделы
- **[01-основы](../01-основы/)** - базовые концепции
- **[02-синтаксис-языка](../02-синтаксис-языка/)** - синтаксические конструкции
- **[03-продвинутые-возможности](../03-продвинутые-возможности/)** - обработка ошибок и отладка
- **[04-обработка-данных](../04-обработка-данных/)** - оптимизация работы с данными
- **[05-типы-данных](../05-типы-данных/)** - система типов

### Финальный раздел
- **[07-демонстрации](../07-демонстрации/)** - комплексные примеры и showcase

### Дополнительные ресурсы
- **[../INDEX.md](../INDEX.md)** - 📋 Быстрый индекс всех примеров
- **[../README.md](../README.md)** - 📚 Главная страница примеров

## 📈 Рекомендуемый порядок изучения

1. **`interactive_demo.dc`** - изучите REPL режим
2. **`debug_mode_test.dc`** - освойте отладку
3. **`stress_benchmark.dc`** - тестирование производительности (осторожно!)

## ⚡ Предупреждения

- **Бенчмарки могут быть медленными** - начните с малых данных
- **Отладочный вывод** может замедлить выполнение
- **REPL режим** - лучший способ изучения языка
- **Измерение производительности** важно для оптимизации

---

**Правильные инструменты делают разработку на DataCode эффективной!** 🛠️✨
