# Тест отладки проблемы с next
# Воспроизводим точную ситуацию из load_model_data.dc

print("============================================================")
print("Тест: Отладка проблемы next в load_model_data.dc")
print("============================================================")

global base_path = getcwd() / "model_data"

print("📂 Базовый путь: ", base_path)
print()

# Получаем список годов
global year_dirs = []
try
    year_dirs = list_files(base_path)
    print("  Найдено элементов в model_data: ", len(year_dirs))
catch e
    print("  ⚠️  Ошибка получения списка файлов: ", e)
endtry
print()

for year_dir in year_dirs do
    # Пропускаем файлы и служебные директории
    if year_dir != '2025' do
        next year_dir
    endif

    print("  🔍 Год ", year_dir)
    
next year_dir