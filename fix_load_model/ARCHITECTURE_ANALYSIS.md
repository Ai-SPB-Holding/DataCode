# Анализ архитектуры и проблема

## ✅ Что работает правильно

1. **Тип возврата**: `Result<Value, DataCodeError>` - используется Result, не panic!
2. **Try/catch**: Реализован через стек исключений (`exception_stack`)
3. **Обработка ошибок**: Ошибки правильно пробрасываются через Result

## ❌ Проблема

Проблема **НЕ в архитектуре try/catch**, а в **парсинге цикла for внутри try блока**.

### Что происходит:

1. Когда `execute_try_statement_directly` извлекает `try_body`, он получает строки:
   ```
       for i in [1, 2, 3] do
           print("i = ", i)
       next i
   ```

2. Затем `execute_block_with_try_support` обрабатывает эти строки
3. Когда она видит `for i in [1, 2, 3] do`, она начинает искать `next i`
4. Но поиск `next i` не находит его правильно

## 🔍 Корневая причина

В функции `execute_block_with_try_support` на строке 1693-1759, логика поиска `next` имеет проблему:

1. Цикл начинается с `j = i` (где `i` - индекс строки `for ... do`)
2. Сначала добавляется строка `for ... do` в `for_lines`
3. Затем `j` увеличивается на 1
4. Но при поиске `next`, возможно цикл завершается преждевременно или не находит `next`

## ✅ Решение

Нужно убедиться, что логика поиска `next` в `execute_block_with_try_support` работает так же, как в `handle_for_loop`.

