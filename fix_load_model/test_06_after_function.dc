# Тест 6: Что происходит после endfunction
print("Тест 6: Что происходит после endfunction")
print("=========================================")

# Функция для объединения таблиц
global function merge_tables(tables) do
    if len(tables) == 0 do
        return null
    endif
    return tables[0]
endfunction

print("После функции merge_tables")

# Пробуем try сразу после функции
try
    print("  Внутри try")
catch e
    print("  Ошибка: ", e)
endtry

print("✓ Тест завершен")

