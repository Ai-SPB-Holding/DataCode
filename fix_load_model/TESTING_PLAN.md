# План тестирования и поиска ошибок

## 📋 Обзор проблемы

**Основная проблема:** Связи между таблицами создаются в DataCode (12 связей), но не экспортируются в SQLite БД (0 связей в `_datacode_relations`).

**Статус:**
- ✅ Связи создаются в памяти через `relate()`
- ✅ Таблица `_datacode_relations` создается в БД
- ❌ Метаданные о связях не записываются в БД
- ✅ Индексы на внешних ключах создаются (14 индексов)
- ✅ Целостность данных проверяется через JOIN'ы

---

## 🔍 Этап 1: Диагностика проблемы

### 1.1 Проверка создания связей в DataCode

**Цель:** Убедиться, что связи действительно создаются в памяти

**Действия:**
```bash
# Запустить загрузку с отладочным выводом
DATACODE_DEBUG=1 cargo run -- load_model_data.dc --build_model 2>&1 | grep -i "relation\|связ"
```

**Ожидаемый результат:**
- Вывод сообщений о создании 12 связей
- Сообщения "✓ Создано связей: 12"

**Проверка:**
- [ ] Связи создаются в реестре (`get_all_relations()`)
- [ ] Функция `relate()` вызывается успешно
- [ ] Нет ошибок при создании связей

---

### 1.2 Проверка экспорта связей в SQLite

**Цель:** Понять, почему связи не записываются в БД

**Действия:**
1. Проверить функцию `write_relations_metadata()` в `src/builtins/sqlite_export.rs`
2. Добавить отладочный вывод в функцию экспорта
3. Проверить маппинг таблиц при экспорте

**Код для проверки:**
```rust
// В src/builtins/sqlite_export.rs, функция write_relations_metadata()
if std::env::var("DATACODE_DEBUG").is_ok() {
    eprintln!("🔍 DEBUG: Found {} relations in registry", relations.len());
    eprintln!("🔍 DEBUG: Found {} tables to match", table_to_var.len());
    // ... существующий код ...
}
```

**Проверка:**
- [ ] Функция `get_all_relations()` возвращает связи
- [ ] Маппинг таблиц работает корректно (`Rc::ptr_eq()`)
- [ ] SQL INSERT выполняется без ошибок
- [ ] Транзакция коммитится

---

### 1.3 Проверка маппинга таблиц

**Цель:** Убедиться, что таблицы правильно сопоставляются при экспорте

**Проблема:** Возможно, таблицы в реестре связей не совпадают с таблицами при экспорте из-за разных `Rc` указателей.

**Действия:**
1. Проверить, как таблицы хранятся в реестре связей
2. Проверить, как таблицы передаются в функцию экспорта
3. Сравнить `Rc` указатели

**Тест:**
```rust
// Добавить в write_relations_metadata()
for (table_rc, var_name, sanitized_name) in &table_to_var {
    eprintln!("  Table: {} -> {}", var_name, sanitized_name);
    eprintln!("    Rc pointer: {:p}", table_rc.as_ptr());
}

for relation in &relations {
    eprintln!("  Relation: {}[{}] <-> {}[{}]", 
        relation.column1, relation.column2, 
        relation.column1, relation.column2);
    eprintln!("    Table1 Rc: {:p}", relation.table1.as_ptr());
    eprintln!("    Table2 Rc: {:p}", relation.table2.as_ptr());
}
```

**Проверка:**
- [ ] `Rc::ptr_eq()` правильно сравнивает таблицы
- [ ] Таблицы из реестра совпадают с таблицами при экспорте
- [ ] Имена таблиц правильно санитизируются

---

## 🧪 Этап 2: Тестирование компонентов

### 2.1 Тест создания связей

**Файл:** `test_relations_creation.dc`

**Цель:** Проверить, что функция `relate()` работает корректно

**Сценарий:**
```datacode
# Создать две тестовые таблицы
global table1 = table([[1, "A"], [2, "B"]], ["id", "name"])
global table2 = table([[1, "X"], [2, "Y"]], ["id", "value"])

# Создать связь
relate(table1["id"], table2["id"])

# Проверить, что связь создана
# (нужно добавить функцию для проверки связей)
```

**Проверка:**
- [ ] Связь создается без ошибок
- [ ] Связь сохраняется в реестре
- [ ] Можно получить связь через `get_all_relations()`

---

### 2.2 Тест экспорта одной связи

**Цель:** Изолировать проблему экспорта

**Действия:**
1. Создать минимальный тест с одной таблицей и одной связью
2. Экспортировать в SQLite
3. Проверить наличие связи в БД

**Тест:**
```datacode
# Минимальный тест экспорта связей
global products = table([[1, "Product 1"]], ["id", "name"])
global sales = table([[1, 1, 100]], ["id", "product_id", "amount"])

relate(products["id"], sales["product_id"])

# Экспорт должен создать связь в БД
```

**Проверка:**
- [ ] Связь экспортируется в БД
- [ ] Запись появляется в `_datacode_relations`
- [ ] Индекс создается

---

### 2.3 Тест последовательности операций

**Цель:** Проверить порядок выполнения операций

**Гипотеза:** Возможно, связи создаются после экспорта или экспорт происходит до создания связей.

**Проверка:**
1. Когда вызывается `export_tables_to_sqlite()`?
2. Когда создаются связи через `relate()`?
3. В каком порядке выполняются операции?

**Действия:**
```bash
# Добавить логирование в main.rs
# Проверить порядок вызовов:
# 1. Выполнение скрипта load_model_data.dc
# 2. Создание связей через relate()
# 3. Вызов export_tables_to_sqlite()
```

**Проверка:**
- [ ] Связи создаются ДО экспорта
- [ ] Экспорт вызывается ПОСЛЕ создания связей
- [ ] Реестр связей не очищается между операциями

---

## 🐛 Этап 3: Поиск и исправление ошибок

### 3.1 Возможные причины проблемы

#### Причина 1: Неправильное сравнение Rc указателей

**Симптомы:**
- Связи создаются в памяти
- `get_all_relations()` возвращает связи
- Но `Rc::ptr_eq()` не находит совпадений

**Решение:**
- Использовать альтернативный способ сравнения таблиц
- Сохранять имена таблиц вместе со связями
- Использовать хеш или уникальный ID таблицы

#### Причина 2: Таблицы пересоздаются при экспорте

**Симптомы:**
- Таблицы в реестре связей - это одни объекты
- Таблицы при экспорте - это другие объекты (новые Rc)

**Решение:**
- Сохранять связи по именам переменных, а не по Rc указателям
- Использовать маппинг имен переменных → имена таблиц в БД

#### Причина 3: Транзакция не коммитится

**Симптомы:**
- INSERT выполняется успешно
- Но данные не сохраняются в БД

**Решение:**
- Проверить коммит транзакции
- Убедиться, что нет отката транзакции

#### Причина 4: Ошибка при записи игнорируется

**Симптомы:**
- Ошибка при INSERT не обрабатывается
- Функция продолжает выполнение без записи

**Решение:**
- Проверить обработку ошибок
- Добавить логирование ошибок

---

### 3.2 План исправлений

#### Исправление 1: Улучшить маппинг таблиц

**Файл:** `src/builtins/sqlite_export.rs`

**Изменения:**
```rust
// Вместо сравнения Rc указателей, использовать имена переменных
// Сохранять в реестре связей имена переменных вместе со связями
```

#### Исправление 2: Добавить отладочный вывод

**Файл:** `src/builtins/sqlite_export.rs`

**Изменения:**
```rust
// Добавить подробное логирование в write_relations_metadata()
// Логировать каждую попытку записи связи
// Логировать успешные и неуспешные записи
```

#### Исправление 3: Проверить порядок операций

**Файл:** `src/main.rs`

**Изменения:**
```rust
// Убедиться, что экспорт вызывается ПОСЛЕ выполнения скрипта
// Проверить, что связи создаются до экспорта
```

---

## ✅ Этап 4: Валидация исправлений

### 4.1 Тесты после исправлений

**Цель:** Убедиться, что исправления работают

**Тесты:**
1. ✅ Запустить `load_model_data.dc --build_model`
2. ✅ Проверить количество связей в БД: `SELECT COUNT(*) FROM _datacode_relations;`
3. ✅ Проверить конкретные связи: `sqlite3 load_model_data.db < test_relations.sql`
4. ✅ Проверить индексы: `SELECT COUNT(*) FROM sqlite_master WHERE type='index' AND name LIKE 'idx_%';`

**Ожидаемый результат:**
- 12 связей в таблице `_datacode_relations`
- Все 12 ожидаемых связей присутствуют
- Индексы созданы для всех связей

---

### 4.2 Регрессионное тестирование

**Цель:** Убедиться, что исправления не сломали другие функции

**Тесты:**
1. ✅ Загрузка данных работает корректно
2. ✅ Объединение таблиц работает
3. ✅ Экспорт таблиц работает
4. ✅ Создание связей работает
5. ✅ Экспорт связей работает

---

### 4.3 Интеграционное тестирование

**Цель:** Проверить весь процесс от начала до конца

**Сценарий:**
```bash
# 1. Очистить БД
rm load_model_data.db

# 2. Загрузить данные
cargo run -- load_model_data.dc --build_model

# 3. Проверить результаты
sqlite3 load_model_data.db < test_db_integrity.sql
sqlite3 load_model_data.db < test_relations.sql

# 4. Проверить целостность данных
sqlite3 load_model_data.db < check_relations.sql
```

**Ожидаемый результат:**
- ✅ Все тесты проходят успешно
- ✅ Все связи присутствуют в БД
- ✅ Целостность данных проверена

---

## 📊 Этап 5: Документация и отчет

### 5.1 Документирование исправлений

**Создать:**
- `FIX_RELATIONS_EXPORT.md` - описание проблемы и решения
- Обновить комментарии в коде
- Добавить примеры использования

### 5.2 Создание тестового набора

**Файлы:**
- `test_relations_creation.dc` - тест создания связей
- `test_relations_export.dc` - тест экспорта связей
- `test_relations.sql` - SQL тест связей (уже создан)
- `test_minimal_relations.dc` - минимальный тест для отладки

---

## 🎯 Приоритеты

### Высокий приоритет
1. ✅ Диагностика проблемы с экспортом связей
2. ✅ Поиск причины отсутствия связей в БД
3. ✅ Исправление экспорта связей

### Средний приоритет
4. ✅ Улучшение отладочного вывода
5. ✅ Создание тестов для связей
6. ✅ Документация исправлений

### Низкий приоритет
7. ✅ Оптимизация экспорта связей
8. ✅ Улучшение обработки ошибок
9. ✅ Расширение функциональности связей

---

## 📝 Чеклист выполнения

### Диагностика
- [ ] Проверка создания связей в DataCode
- [ ] Проверка экспорта связей в SQLite
- [ ] Проверка маппинга таблиц
- [ ] Проверка порядка операций

### Исправления
- [ ] Исправление маппинга таблиц
- [ ] Добавление отладочного вывода
- [ ] Проверка порядка операций
- [ ] Обработка ошибок

### Тестирование
- [ ] Тест создания связей
- [ ] Тест экспорта одной связи
- [ ] Тест последовательности операций
- [ ] Регрессионное тестирование
- [ ] Интеграционное тестирование

### Валидация
- [ ] Проверка количества связей в БД
- [ ] Проверка конкретных связей
- [ ] Проверка индексов
- [ ] Проверка целостности данных

### Документация
- [ ] Документирование исправлений
- [ ] Создание тестового набора
- [ ] Обновление комментариев в коде

---

## 🔧 Инструменты для отладки

### Отладочные команды

```bash
# Включить отладочный вывод
export DATACODE_DEBUG=1

# Запустить загрузку с отладкой
cargo run -- load_model_data.dc --build_model 2>&1 | tee debug_output.log

# Проверить связи в БД
sqlite3 load_model_data.db "SELECT * FROM _datacode_relations;"

# Проверить количество связей
sqlite3 load_model_data.db "SELECT COUNT(*) FROM _datacode_relations;"

# Проверить индексы
sqlite3 load_model_data.db "SELECT name FROM sqlite_master WHERE type='index' AND name LIKE 'idx_%';"
```

### SQL запросы для диагностики

```sql
-- Проверить структуру таблицы связей
.schema _datacode_relations

-- Проверить все связи
SELECT * FROM _datacode_relations;

-- Проверить связи по таблицам
SELECT from_table, COUNT(*) as count 
FROM _datacode_relations 
GROUP BY from_table;

-- Проверить индексы
SELECT name, tbl_name, sql 
FROM sqlite_master 
WHERE type='index' AND name LIKE 'idx_%';
```

---

## 📅 Временная оценка

- **Диагностика:** 2-4 часа
- **Исправления:** 4-6 часов
- **Тестирование:** 2-3 часа
- **Валидация:** 1-2 часа
- **Документация:** 1-2 часа

**Итого:** 10-17 часов

---

## 🎓 Выводы и рекомендации

1. **Немедленные действия:**
   - Включить отладочный вывод (`DATACODE_DEBUG=1`)
   - Проверить маппинг таблиц при экспорте
   - Проверить порядок операций

2. **Долгосрочные улучшения:**
   - Улучшить систему связей (сохранять имена переменных)
   - Добавить автоматические тесты для связей
   - Улучшить обработку ошибок при экспорте

3. **Мониторинг:**
   - Добавить метрики экспорта связей
   - Логировать успешные и неуспешные экспорты
   - Создать дашборд для мониторинга связей

